{
  "private": true,
  "license": "UNLICENSED",
  "name": "mfe-boilerplate",
  "version": "0.0.1",
  "scripts": {
    "preinstall": "yarn --cwd backend && yarn --cwd frontend",
    "postinstall": "husky install",
    "start": "run-p start:**",
    "start:backend": "yarn --cwd ./backend start",
    "start:frontend": "yarn --cwd ./frontend start"
  },
  "devDependencies": {
    "husky": "6.0.0",
    "npm-run-all": "4.1.5"
  }
}
