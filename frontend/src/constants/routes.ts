export const routes = {
  home: '/',
  store: '/store',
  categories: '/store/:categorySlug',
  products: '/store/:categorySlug/:subCategorySlug',
  product: '/product/:productSlug',
  checkout: '/checkout',
  cart: '/cart',
};
